<?php
/**
* Partial functions
*
* @package Theme Palace
* @subpackage Top Travel Pro
* @since Top Travel Pro 1.0.0
*/


if ( ! function_exists( 'top_travel_slider_btn_label_partial' ) ) :
    // about title
    function top_travel_slider_btn_label_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['slider_btn_label'] );
    }
endif;


if ( ! function_exists( 'top_travel_about_title_partial' ) ) :
    // about title
    function top_travel_about_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['about_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_about_sub_title_partial' ) ) :
    // about sub title
    function top_travel_about_sub_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['about_sub_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_about_description_partial' ) ) :
    // about description
    function top_travel_about_description_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['about_description'] );
    }
endif;

if ( ! function_exists( 'top_travel_about_btn_title_partial' ) ) :
    // about btn title
    function top_travel_about_btn_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['about_btn_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_service_title_partial' ) ) :
    // about btn title
    function top_travel_service_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['service_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_service_sub_title_partial' ) ) :
    // service_sub_title
    function top_travel_service_sub_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['service_sub_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_service_btn_title_partial' ) ) :
    // service_btn_title
    function top_travel_service_btn_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['service_btn_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_package_title_partial' ) ) :
    // package_title
    function top_travel_package_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['package_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_package_sub_title_partial' ) ) :
    // package_sub_title
    function top_travel_package_sub_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['package_sub_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_package_btn_title_partial' ) ) :
    // package_btn_title
    function top_travel_package_btn_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['package_btn_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_popular_destination_btn_title_partial' ) ) :
    // popular_destination_btn_title
    function top_travel_popular_destination_btn_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['popular_destination_btn_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_testimonial_section_title_partial' ) ) :
    // testimonial_section_title
    function top_travel_testimonial_section_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['testimonial_section_title'] );
    }
endif;

if ( ! function_exists( 'top_travel_testimonial_section_sub_title_partial' ) ) :
    // about btn title
    function top_travel_testimonial_section_sub_title_partial() {
        $options = top_travel_get_theme_options();
        return esc_html( $options['testimonial_section_sub_title'] );
    }
endif;









